---
  - hosts: debian
    become: true

    tasks:
      - name: Install Apache Server 
        action: apt name=apache2 state=installed

      - name: Start Server Apache
        action: service name=apache2 state=restarted